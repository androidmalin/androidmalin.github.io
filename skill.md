# Jekyll 博客文章格式化 Skill

## 作用

指导在 Jekyll 博客项目（al-folio 主题）中，对 `_posts/` 目录下的 Markdown 文件进行标准化处理，包括文件名重命名和内容头部 YAML front matter 的添加/修正。

---

## 适用场景

- 新撰写的博客文章未按 Jekyll 规范命名
- 文件缺少 YAML front matter（文章头信息）
- 文件头部信息格式错误或字段缺失
- 从外部导入的 Markdown 文件需要适配本博客格式

---

## 1. 文件名重命名规则

### 命名格式

```
YYYY-MM-DD-<title>.md
```

### 各部分说明

| 部分 | 说明 | 示例 |
|---|---|---|
| `YYYY-MM-DD` | 文章发布日期，必须使用 ISO 8601 格式 | `2026-05-18` |
| `<title>` | 文章标题的英文/拼音/简短形式，小写，单词间用连字符 `-` 分隔 | `skills-analysis-report` |
| `.md` | 文件扩展名，必须为 `.md` 或 `.markdown` | `.md` |

### 日期确定优先级

1. **文件内容中明确提到的日期**（如正文中的"分析时间：2026-05-18"）
2. **文件本身的创建时间或修改时间**
3. **当前日期**（仅作为最后备选）

### 标题转换规则

- 将中文标题转换为拼音或英文缩写
- 移除无意义的虚词（的、了、在、与、及等）
- 全部使用小写字母
- 单词之间用连字符 `-` 连接
- 避免特殊字符（如 `:`、`?`、`/`、`\` 等）

**示例转换：**

| 原标题 | 转换后文件名 |
|---|---|
| Skills 分析报告 | `2026-05-18-skills-analysis-report.md` |
| Android Gradle Plugin 升级指南 | `2026-04-15-android-gradle-plugin-upgrade-guide.md` |
| 如何实施模块化 | `2026-04-09-how-to-implement-modularization.md` |

---

## 2. YAML Front Matter 添加规则

### 必需字段

文件开头必须包含以下 YAML front matter（三个连字符 `---` 包裹）：

```yaml
---
layout: post
title: "文章标题"
date: YYYY-MM-DD HH:MM:SS +0800
tags: []
---
```

### 字段说明

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `layout` | string | 是 | 固定为 `post`，表示使用博客文章布局 |
| `title` | string | 是 | 文章标题，用双引号包裹。优先从原文件的首个一级标题 `# 标题` 提取 |
| `date` | string | 是 | 日期时间，格式 `YYYY-MM-DD HH:MM:SS +0800`。时间部分无具体信息时默认 `00:00:00`，时区固定为 `+0800` |
| `tags` | array | 是 | 文章标签，无标签时留空数组 `[]`。有标签时填写字符串数组，如 `[android, gradle]` |

### 提取规则

1. **标题提取**：
   - 优先从原文件的首个一级 Markdown 标题 `# 标题内容` 提取
   - 若无一级标题，则根据文件主题概括生成
   - 提取后，原正文中的 `# 标题` 可保留或删除（建议保留，与 front matter 中的 `title` 一致时保持正文原样即可）

2. **日期提取**：
   - 优先从文件内容中查找明确日期（如 `分析时间：2026-05-18`）
   - 其次使用文件系统时间戳
   - 格式必须补全为 `YYYY-MM-DD 00:00:00 +0800`

3. **标签提取**：
   - 若原文未提供标签，默认填写 `tags: []`
   - 若原文有标签信息，转换为 YAML 数组格式

---

## 3. 处理流程

遇到需要格式化的博客文章时，按以下步骤执行：

1. **读取原文件**
   - 查看当前文件名
   - 查看文件内容，识别一级标题和日期信息

2. **确定新文件名**
   - 按「文件名重命名规则」生成 `YYYY-MM-DD-<title>.md`

3. **构建 YAML Front Matter**
   - 按「YAML Front Matter 添加规则」构建头部信息
   - 将 front matter 插入到文件最开头

4. **生成新文件**
   - 将处理后的内容写入新文件

5. **清理旧文件**
   - 删除原始未格式化的文件

6. **验证结果**
   - 确认新文件存在
   - 确认头部格式正确（`---` 包裹，字段缩进正确）
   - 确认文件名符合规范

---

## 4. 示例

### 修改前

**文件名：** `skills.md`

**内容：**
```markdown
# Skills 分析报告

> 分析目录：`/Users/malin/.claude/skills`
> 分析时间：2026-05-18

---

## 1. adaptive

**作用**：指导如何使 Android 应用的 UI 适配不同设备形态...
```

### 修改后

**文件名：** `2026-05-18-skills-analysis-report.md`

**内容：**
```markdown
---
layout: post
title: "Skills 分析报告"
date: 2026-05-18 00:00:00 +0800
tags: []
---

# Skills 分析报告

> 分析目录：`/Users/malin/.claude/skills`
> 分析时间：2026-05-18

---

## 1. adaptive

**作用**：指导如何使 Android 应用的 UI 适配不同设备形态...
```

---

## 5. 注意事项

1. **必须删除旧文件**：处理完成后，务必删除原始不规范的文件，避免 Jekyll 生成重复或冲突的文章。

2. **时间格式严格**：`date` 字段必须包含时区 `+0800`，否则 Jekyll 可能解析错误。

3. **保留正文内容**：添加 front matter 时，不要修改或删除正文原有内容，仅在文件最开头插入 YAML 块。

4. **引号处理**：如果文章标题中包含 YAML 特殊字符（如 `:`、`"`、`|` 等），`title` 字段必须用双引号包裹。

5. **分隔符处理**：原正文中的 `---`（水平分隔线）与 YAML front matter 的 `---` 不冲突，YAML 块只出现在文件最开头。

6. **文件扩展名**：统一使用 `.md`，若原文件为 `.markdown`，重命名时可保持 `.markdown` 或改为 `.md`（本项目统一使用 `.md`）。

---

## 6. 快速检查清单

处理完成后，对照以下清单确认：

- [ ] 文件名格式为 `YYYY-MM-DD-<title>.md`
- [ ] 文件以 `---` 开头
- [ ] 包含 `layout: post`
- [ ] 包含 `title: "..."`
- [ ] 包含 `date: YYYY-MM-DD HH:MM:SS +0800`
- [ ] 包含 `tags: []`
- [ ] 第二个 `---` 之后紧跟正文内容
- [ ] 旧文件已删除
